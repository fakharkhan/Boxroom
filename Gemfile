
source 'http://rubygems.org'

gem 'rake'
gem 'rails', '3.2.6'
gem 'jquery-rails'
gem 'dynamic_form'
gem 'acts_as_tree'
gem 'paperclip'

#gem 's3_swf_upload', :git => 'git://github.com/mischa78/s3-swf-upload-plugin.git'
#gem 'aws-sdk'
#gem 'xml-simple'
gem 'aws-s3'

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails', '~> 3.2.3'
  gem 'coffee-rails', '~> 3.2.1'
  gem 'uglifier', '>= 1.0.3'
end

group :development, :test do
  gem 'mysql2'
  gem 'factory_girl_rails'
end
group :production do
  gem 'pg'
end